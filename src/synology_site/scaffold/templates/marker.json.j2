{
  "tool": "synology-site-deployer",
  "version": "{{ version }}",
  "domain": "{{ domain }}",
  "slug": "{{ slug }}",
  "framework": "{{ framework }}",
  "port": {{ port }},
  "container": "{{ slug }}"
}
